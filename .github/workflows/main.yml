name: Build iOS Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Test message
        run: echo "Workflow funcionando correctamente 🔥"
